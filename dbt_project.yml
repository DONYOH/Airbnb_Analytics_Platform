name: 'airbnb_project'
version: '1.0.0'
config-version: 2

profile: 'airbnb_project'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  airbnb_project:
    bronze:
      materialized: view